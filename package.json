{
  "name": "git-clone-select",
  "version": "1.0.0",
  "description": "A global npm package to easily clone git repositories into your Projects folder",
  "main": "index.js",
  "bin": {
    "git-clone-select": "./bin/git-clone.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "git",
    "clone",
    "repository",
    "cli"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "inquirer": "^8.2.6",
    "chalk": "^4.1.2"
  }
}
